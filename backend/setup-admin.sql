-- Скрипт для установки пароля администратора из переменной окружения
-- ВАЖНО: Этот скрипт должен выполняться через Node.js скрипт, который читает .env
-- 
-- Для ручного выполнения используйте хеш пароля, который можно получить так:
-- node -e "const bcrypt = require('bcrypt'); bcrypt.hash('ваш_пароль', 10).then(h => console.log(h));"

-- Пример обновления пароля админа для телефона +71262342332
-- Замените хеш на значение из ADMIN_PASSWORD в .env, захешированное через bcrypt

-- UPDATE users 
-- SET password = '$2b$10$ваш_хеш_пароля_из_env'
-- WHERE role = 'admin' AND phone = '+71262342332';

-- Или для всех админов:
-- UPDATE users 
-- SET password = '$2b$10$ваш_хеш_пароля_из_env'
-- WHERE role = 'admin';
